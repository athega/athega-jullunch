%script#ads_tpl{ type: 'text/html' }
  #large_ad.sixteen.columns
    %img{ src: "{{random_url}}" }

%script#check_ins_tpl{ type: 'text/html' }
  .sixteen.columns
    %h1 Senast incheckade gäster
  .row
    %ul.check_in
      {{#items}}
      %li.one-third.column
        %img{ src: "{{image_url}}", width: 300, height: 400 }
        %br
        %span.user {{name}}
        %span.timestamp{ data: { timestamp: "{{arrived_at}}" } } {{arrived_at}}
        %div
          %small.company ({{company}})
      {{/items}}

%script#images_tpl{ type: 'text/html' }
  .sixteen.columns.images_header
    %h1 Senaste bilderna
  .row
    %div.images
      {{#items}}
      .one-third.column
        %img{ src: '{{url}}', width: 300, height: 300 }
      {{/items}}

%script#tweets_tpl{ type: 'text/html' }
  .sixteen.columns
    %h1
      Tweets taggade
      %a{ href: 'https://twitter.com/#!/search/realtime/%23athegajul' } #athegajul
  .sixteen.columns.alpha.omega
    %ul.tweets
      {{#items}}
      %li.tweet.row
        .two.columns
          %img{ src: "{{#user}}{{profile_image_url_https}}{{/user}}", width: 100, height: 100 }
        .ten.columns
          %a.user{ href: "https://twitter.com/{{#user}}{{screen_name}}{{/user}}"} {{#user}}{{screen_name}}{{/user}}
          %span.name {{#user}}{{name}}{{/user}}
        .thirteen.columns
          %span.text {{{html}}}
          {{#entities}}{{#media}}
          %img{ src: "{{media_url}}", width: "{{#sizes}}{{#small}}{{w}}{{/small}}{{/sizes}}", height: "{{#sizes}}{{#small}}{{h}}{{/small}}{{/sizes}}" }
          {{/media}}{{/entities}}
          %div.timestamp{ data: { timestamp: "{{created_at}}" } } {{created_at}}
      {{/items}}
