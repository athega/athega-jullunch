.row.notifications
  .twelve.offset-by-four.columns
    %h1 Hantera notiﬁeringar
  .three.columns
    = haml :'admin/menu', :layout => false
  .twelve.offset-by-one.columns
    - unless params[:number_of_invitations_sent].nil?
      %div.alert-message
        Skickade
        %strong= params[:number_of_invitations_sent]
        inbjudningar!

    %form{ action: '/admin/notifications/send_all_pending_invitations', method: 'POST' }
      %label.half-bottom
        Bjud in alla gäster som inte redan fått en inbjudan.
        %small (Max 1 mail per gäst)
      %input{ type: 'submit', value: 'Skicka inbjudningar', onclick: 'return confirm("Är du HELT säker på att du vill skicka inbjudningarna?");' }

    %hr

    %form{ action: '/admin/notifications/send_all_pending_reminders', method: 'POST' }
      %label.half-bottom
        Skicka en påminnelse till alla gäster som inte redan fått en.
        %small (Max 1 mail per gäst)
      %input{ type: 'submit', value: 'Skicka påminnelser', onclick: 'return confirm("Är du HELT säker på att du vill skicka påminnelserna?");' }
